// Memory provider abstraction removed — the team harness now uses
// flopsygraph's unified BaseStore (memory_search + memory tools) directly.
// Sessions still trigger skill extraction via SessionExtractor in handler.ts.
